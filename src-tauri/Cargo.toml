[package]
name = "neuronos"
version = "0.1.0"
description = "NeuronOS local AI desktop assistant"
authors = ["NeuronOS"]
edition = "2021"

[lib]
name = "neuronos_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-shell = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
